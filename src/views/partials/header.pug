header
    div
        span.headerLine
            i.fas.fa-bars
            a(href="/").header__logo
                i.fab.fa-youtube
                b iutube
        div.search__div
            form(method="GET").search__form
                input(type="text" placeholder="Search by title" name="keyword")#search
                button
                    i.fas.fa-search
        nav
            ul            
                if loggedIn
                    li
                        a(href="/videos/upload") Upload Video
                    li
                        a(href=`/users/${loggedInUser._id}`) My Profile
                    li
                        a(href="/users/edit") Edit Profile
                    li
                        a(href="/users/logout")  Log Out
                    li
                        a(href=`/users/${loggedInUser._id}`)
                            if !loggedInUser.avatarUrl
                                span 😀
                            else
                                if loggedInUser.avatarUrl
                                    if loggedInUser.avatarUrl.includes("github")
                                        img.header__avatar(crossorigin src=loggedInUser.avatarUrl)
                                    else
                                        img.header__avatar(crossorigin src=loggedInUser.avatarUrl)
                else
                    li
                        a(href="/login")  Login                        
                    li
                        a(href="/join").header__btn  Join